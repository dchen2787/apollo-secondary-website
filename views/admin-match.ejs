<%- include("./partials/header"); -%>
<div class="container">
  <% if (errM) { %><div class="alert alert-info"><%= errM %></div><% } %>

  <div class="admin-wrap">
    <aside class="cardish" style="position:sticky;top:16px;align-self:start;">
      <h5 style="margin:0 0 8px 0;">Admin</h5>
      <div class="nav">
        <a href="/admin/students">Students</a>
        <a href="/admin/match" class="active">Match Controls</a>
      </div>
    </aside>

    <main>
      <section class="cardish" style="display:flex;align-items:center;gap:10px">
        <h2 style="margin:0;font-size:20px;">Match Controls</h2>
      </section>

      <!-- In-page nav -->
      <nav class="cardish" style="display:flex;gap:10px;flex-wrap:wrap">
        <a class="btn btn-light btn-sm" href="#phase-lock">Phase & Lock</a>
        <a class="btn btn-light btn-sm" href="#confirm-controls">Confirm Controls</a>
        <a class="btn btn-light btn-sm" href="#shadow-slots">Shadow Slots</a>
      </nav>

      <section id="phase-lock" class="cardish">
        <h3 style="margin-top:0">Phase & Lock</h3>
        <!-- Your form for /admin-matchSettings (phase + lock). Remove any "matching allowed for ..." text here -->
        <!-- ... -->
      </section>

      <section id="confirm-controls" class="cardish">
        <h3 style="margin-top:0">Confirm Controls</h3>
        <!-- buttons: reset all, clear one, etc. -->
        <!-- ... -->
      </section>

      <section id="shadow-slots" class="cardish">
        <h3 style="margin-top:0">Shadow Slots</h3>
        <!-- slot importing / managing UI -->
        <!-- ... -->
      </section>
    </main>
  </div>
</div>
<%- include("./partials/footer"); -%>
